// Título desta página:
pageTitle = 'Faça contato';

// Aplicativo desta página:
function runPage() {

    // Altera o <title> do documento:
    setTitle(pageTitle);

    // Faz alguma coisa inútil só para testar:
    console.log('contactinho');
}

// Executa o aplicativo desta página:
runPage();